1, The difference between 'Undefined' and 'Not defined'.
    Undefined means a variable is not given a particular value for instance 
    var x; this variable x is un defines
    while Not defined means a variable is not declared let alone been given a value.
    
2, 1 undefined, 1undefined, 1function

3, The drawback of using a private method is that there is insufficient memory, only few data can be added.

4, Closures are functions that are enclosed inside another function and function that has to its own private variables.

5, function mull(a, b, c){
    return a*b*c;
}
    mull(2, 3, 4);
    mull(4, 3, 4);
    
6, arraylist=[ ];

7, var items=['Bags', 'Belt', 'Shoe', 'Short', 'Boxers'];
    if(items === array){
    console.log(true);
    }else{
    console.log("items are not array");
    }
    
8, 0

9, No value or 0 or undefined

10, No value or 0 or undefined

11, xyz

12, 

13, 5

14, 1, 1 xyz, 2, 1

15, undefined

16, no output

17, fuction foo is run time function while function bar is a parse time function.

18, it's a behaviour of moving declaration to the top the current of scoop.

19, Original salary was undefined
    My salary is 5000$
    
20, false

21, console.log(counterArray.lenght);
    